# ============================================
# VARIABLES DE ENTORNO - vkApp 2.0
# ============================================
# Copia este archivo como '.env' y actualiza los valores

# --- SECRET KEY ---
# Genera una clave secreta única para producción:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=tu-clave-secreta-super-segura-aqui

# --- SUPABASE DATABASE CONNECTION ---
# Encuentra estos valores en: Supabase Dashboard > Project Settings > Database > Connection String

# Opción 1: Connection String completa (RECOMENDADO)
SUPABASE_DB_URI=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# --- MODO DE BASE DE DATOS ---
# False = Usa Supabase (por defecto)
# True = Usa PostgreSQL local (respaldo)
USE_LOCAL_DB=False
